{
    "host" : "&{smtp.host}",
    "port" : "&{smtp.port}",
    "auth" : {
        "enable" : true,
        "username" : "&{smtp.username}",
        "password" : "&{smtp.password}"
    },
    "starttls" : {
        "enable" : true
    },
    "from" : "&{smtp.from}"
}